apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: devops-challenge
  labels:
    app: api-service
spec:
  type: ClusterIP
  ports:
  - port: 9999
    targetPort: 9999
    protocol: TCP
    name: http
  selector:
    app: api-service
